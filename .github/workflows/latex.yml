name: Build All LaTeX Documents
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    
    steps:
      # ===== 1️⃣ Récupération du dépôt =====
      - name: Checkout repository
        uses: actions/checkout@v4

      # ===== 2️⃣ Cache LaTeX optimisé =====
      - name: Restore LaTeX cache
        uses: actions/cache/restore@v4
        with:
          path: |
            /usr/share/texlive
            ~/.texmf-var
          key: texlive-${{ runner.os }}-full-v2
          restore-keys: |
            texlive-${{ runner.os }}-full-
            texlive-${{ runner.os }}-

      # ===== 3️⃣ Installation LaTeX complète =====
      - name: Set up LaTeX environment
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            texlive-full \
            latexmk \
            cm-super \
            dvipng
          
          echo "✅ LaTeX installé avec succès"
          pdflatex --version | head -1

      # ===== 4️⃣ Configuration latexmk =====
      - name: Configure latexmk
        run: |
          mkdir -p ~/.config/latexmk
          cat > ~/.latexmkrc << 'EOF'
          $pdflatex = 'pdflatex -interaction=nonstopmode -halt-on-error %O %S';
          $max_repeat = 3;
          $clean_ext = "aux fls fdb_latexmk log out toc figlist makefile synctex.gz";
          $out_dir = '.';
          $silent = 0;
          EOF
          
          echo "✅ Configuration latexmk créée"

 # ===== 5️⃣ Compilation des fichiers LaTeX =====
      - name: Compile all LaTeX files
        shell: bash  # <--- AJOUTEZ CETTE LIGNE
        run: |
          echo "🔍 Démarrage de la compilation..."
          echo ""

          echo "📂 Vérification initiale : fichiers détectés dans le dépôt"
          echo "--------------------------------------------------------"
          find . -type f -name "*.tex" -not -path "./.git/*" | sort || echo "(aucun fichier trouvé)"
          echo "--------------------------------------------------------"
          echo ""

          TOTAL_FILES=0
          COMPILED_COUNT=0
          FAILED_COUNT=0
          ERRORS_LOG="build_errors.log"
          
          # Initialiser le fichier d'erreurs
          : > "$ERRORS_LOG"
          
          # Patterns à compiler (dans l'ordre)
          # Note: "SheetCheat.tex" était peut-être "Cheatsheet.tex" ? J'ai gardé le vôtre.
          PATTERNS=("Cours.tex" "TD.tex" "Corrigé.tex" "TP.tex" "SheetCheat.tex" "Poster.tex" "Diapo.tex")
          
          # Boucle sur chaque pattern
          for pattern in "${PATTERNS[@]}"; do
            echo "📋 Recherche des fichiers: $pattern"
            
            # Trouver tous les fichiers correspondants (exclure .git)
            # Utilisation de find ... -print0 | while read -d '' ... pour gérer les espaces
            find . -type f -name "$pattern" -not -path "./.git/*" -print0 | while IFS= read -r -d '' file; do
              ((TOTAL_FILES++))
              
              dir=$(dirname "$file")
              name=$(basename "$file" .tex)
              # $GITHUB_WORKSPACE est déjà le répertoire de travail, pas besoin de l'ajouter
              
              echo ""
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              echo "📄 [$TOTAL_FILES] Compilation: $file"
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              
              # Aller dans le répertoire du fichier
              cd "$dir" || { echo "Erreur: impossible d’entrer dans $dir"; continue; }
              
              # Nettoyer avant
              latexmk -c "$name.tex" 2>/dev/null || true
              
              if timeout 480 latexmk -pdf "$name.tex" > "$name.tmp.log" 2>&1; then
                if [ -f "$name.pdf" ]; then
                  ((COMPILED_COUNT++))
                  size=$(ls -lh "$name.pdf" | awk '{print $5}')
                  echo "✅ Succès: $name.pdf généré ($size)"
                  mv "$name.tmp.log" "$name.log"
                else
                  ((FAILED_COUNT++))
                  echo "❌ Erreur: PDF non généré (check $name.log)"
                  mv "$name.tmp.log" "$name.log"
                  echo "FAILED: $file - PDF non généré" >> "$GITHUB_WORKSPACE/$ERRORS_LOG"
                fi
              else
                ((FAILED_COUNT++))
                echo "❌ Erreur: Compilation échouée ou timeout (8 min)"
                echo "FAILED: $file - Timeout ou erreur de compilation" >> "$GITHUB_WORKSPACE/$ERRORS_LOG"
                if [ -f "$name.tmp.log" ]; then
                  mv "$name.tmp.log" "$name.log"
                fi
              fi
              
              # Retourner au répertoire de base
              cd "$GITHUB_WORKSPACE" || exit 1
            
            done # Fin de la boucle while
          done # Fin de la boucle for
          
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 RÉSUMÉ DE COMPILATION"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Total fichiers trouvés: $TOTAL_FILES"
          echo "✅ Succès: $COMPILED_COUNT"
          echo "❌ Erreurs: $FAILED_COUNT"
          echo ""
          
          echo "📋 PDFs générés:"
          # Re-créer la liste des patterns PDF
          PDF_PATTERNS=()
          for p in "${PATTERNS[@]}"; do PDF_PATTERNS+=(-o -name "${p%.tex}.pdf"); done
          # Enlever le premier "-o" qui est en trop
          find . -type f \( "${PDF_PATTERNS[@]:1}" \) -not -path "./.git/*" | while read -r f; do
            size=$(ls -lh "$f" | awk '{print $5}')
            echo "   $f ($size)"
          done
          
          echo ""
          
          if [ -s "$ERRORS_LOG" ]; then
            echo "⚠️  FICHIERS AVEC ERREURS:"
            cat "$ERRORS_LOG" | sed 's/^/   /'
            echo ""
          fi
          
          echo "TOTAL_FILES=$TOTAL_FILES" >> $GITHUB_ENV
          echo "COMPILED_COUNT=$COMPILED_COUNT" >> $GITHUB_ENV
          echo "FAILED_COUNT=$FAILED_COUNT" >> $GITHUB_ENV

      # ===== 5️⃣ bis Sauvegarder le cache LaTeX =====
      - name: Save LaTeX cache
        uses: actions/cache/save@v4
        if: always()
        with:
          path: |
            /usr/share/texlive
            ~/.texmf-var
          key: texlive-${{ runner.os }}-full-v2
      - name: Archive error logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs
          path: |
            **/*.log
            build_errors.log
          retention-days: 7

      # ===== 7️⃣ Commit et push des PDFs =====
      - name: Commit and push PDFs
        if: always()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          echo "📍 Vérification des PDFs avant commit..."
          find . -type f \( -name "Cours.pdf" -o -name "TD.pdf" -o -name "Corrigé.pdf" -o -name "TP.pdf" -o -name "SheetCheat.pdf" -o -name "Poster.pdf" -o -name "Diapo.pdf" \) -not -path "./.git/*" | sort
          
          # Ajouter tous les PDFs générés
          git add "**/*.pdf" || true
          
          # Vérifier les fichiers en attente
          echo ""
          echo "📍 Fichiers en attente de commit:"
          git status --short
          
          # Commit si changements
          if ! git diff --quiet --cached; then
            git commit -m "📄 Auto-generate PDFs [${{ env.COMPILED_COUNT }}✅ / ${{ env.TOTAL_FILES }}] [skip ci]"
            echo "✅ Commit créé"
            
            # Push avec retry
            for i in {1..3}; do
              if git push origin HEAD:${{ github.ref }}; then
                echo "✅ Push réussi"
                break
              else
                echo "⚠️  Tentative $i/3 échouée, nouvelle tentative..."
                sleep 5
              fi
            done
          else
            echo "ℹ️  Aucun changement à commiter"
          fi

      # ===== 8️⃣ Notification résumée =====
      - name: Summary
        if: always()
        run: |
          echo "## 📊 Résumé de la compilation LaTeX" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Métrique | Valeur |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Total fichiers | ${{ env.TOTAL_FILES }} |" >> $GITHUB_STEP_SUMMARY
          echo "| ✅ Succès | ${{ env.COMPILED_COUNT }} |" >> $GITHUB_STEP_SUMMARY
          echo "| ❌ Erreurs | ${{ env.FAILED_COUNT }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ env.FAILED_COUNT }}" -gt 0 ]; then
            echo "⚠️ **Des compilations ont échoué. Vérifiez les logs.**" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ **Toutes les compilations ont réussi !**" >> $GITHUB_STEP_SUMMARY
          fi
